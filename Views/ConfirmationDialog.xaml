<Window x:Class="RamDump.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner">
    <Border Background="{StaticResource MantelBrush}"
            BorderBrush="{StaticResource Surface1Brush}"
            BorderThickness="1" CornerRadius="8"
            Padding="24,20" MinWidth="360" MaxWidth="480">
        <StackPanel>
            <TextBlock x:Name="MessageText" TextWrapping="Wrap"
                       Foreground="{StaticResource TextBrush}"
                       FontSize="14" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="YesButton" Content="Ja, bereinigen"
                        Padding="16,8" Margin="0,0,8,0" IsDefault="True">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource LavenderColor}" Opacity="0.2"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="BorderBrush" Value="{StaticResource LavenderBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource LavenderBrush}"/>
                        </Style>
                    </Button.Style>
                </Button>
                <Button x:Name="NoButton" Content="Abbrechen"
                        Padding="16,8" IsCancel="True"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
